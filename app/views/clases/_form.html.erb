<%= form_with(model: clase) do |form| %>
  <% if clase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clase.errors.count, "error") %> prohibited this clase from being saved:</h2>

      <ul>
        <% clase.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :video_link %>
    <%= form.text_field :video_link %>
  </div>

  <div class="field">
    <%= form.label :text_transcription %>
    <%= form.text_field :text_transcription %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
